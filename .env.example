# Root-level local environment template
# Keep this file as an example only.

# Main app runtime toggles (flutter --dart-define)
STORY_AGENT_URL=http://localhost:8080/
USE_MOCK_STORIES=true
APPCHECK_REQUIRED=false

# Admin app runtime toggles (flutter --dart-define)
USE_MOCK_ADMIN=true
